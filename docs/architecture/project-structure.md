# Project Structure

- `src/app`：应用启动、根组件、全局样式入口。
- `src/views`：页面编排、页面布局与 section 组合。
- `src/features`：业务子域（navigation、dashboard、profile）及其模型与业务 UI。
- `tests/resource-center`：按 `model / ui / smoke` 分层的验证入口。
- `docs/architecture`：结构规则与边界约束。
- `docs/adr`：关键架构决策记录。
